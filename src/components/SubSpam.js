import styled from "styled-components";
import cores from "./Cores";

const SpamSublinhado = styled.span`
color: ${cores.corTexto};
margin: 5px;
text-decoration: underline;
`;

export default SpamSublinhado;